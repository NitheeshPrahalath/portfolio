---
title: "What is JSX?"
date: "2026-05-22"
description: "Understanding JSX and why React uses it"
---

JSX looks like HTML but it's actually JavaScript.

## Why JSX?

It lets you write your UI and logic in the same place, which makes components easier to reason about.

## Example

Instead of creating elements manually, you just write what looks like HTML and React handles the rest.